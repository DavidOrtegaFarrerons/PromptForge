info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{api-gateway-domain}}/{{auth-endpoint}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
