info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{api-gateway-domain}}/{{auth-endpoint}}/login"
  body:
    type: json
    data: |-
      {
        "email": "david@example.com",
        "password": "secret"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
